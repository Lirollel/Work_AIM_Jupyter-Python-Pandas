select * from "RISKACCESS"."sapPositionArrears"
where "reportDate" = TO_DATE('29/12/23', 'DD/MM/YY')

это табличка, которая выгружается из SAP (это основная корпоративная ERP система) 
в хранилище (DWH, база данных, к которой ты подключаешься).
Табличка содержит данные по кредитам, займам, гарантиям и аккредитивам.


Сейчас нас интересуют только кредиты и займы, причём только внутригрупповые по группе СУЭК.
эта информация используется в расчётном файле на закладке debt

	
Credit_portfolio_SAP.xlsx - файлик, в котором я перекладывал данные из таблицы sapPositionArrears
Выгрузка на Export Worksheet, затем сводная таблица, Лист2 кажется, затем копи пэйст на лист aggregated


Долг из квантума
select * from "RISKACCESS"."quantumDebt" where "effectiveFrom" = TO_DATE('29/02/24', 'DD/MM/YY')


select * from "RISKACCESS"."quantumForwards" where "positionDate" = TO_DATE('29/02/24', 'DD/MM/YY')
select * from "RISKACCESS"."quantumSwaps" where "positionDate" = TO_DATE('29/02/24', 'DD/MM/YY')